// Mock for SVG files
import React from 'react';

// This mock will render a simple div with the SVG filename as its content
export const ReactComponent = 'div';
const mock = 'SvgrURL';

export default mock;
